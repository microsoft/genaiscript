diff --git a/packages/core/src/diff.ts b/packages/core/src/diff.ts
index f0d88536..889b6379 100644
--- a/packages/core/src/diff.ts
+++ b/packages/core/src/diff.ts
@@ -1,4 +1,5 @@
 import { assert } from "./util"
+import parseUniDiff from "parse-diff"
 
 export interface Chunk {
     state: "existing" | "deleted" | "added"
@@ -7,7 +8,8 @@ export interface Chunk {
 }
 
 /**
- * The LLMD diff format is a simple format that can be used to represent changes. It is not precise:
+ * The LLMD diff format is a simple format that can be used to represent changes. 
+ * It is not precise:
  * - indentation may be lost
  * - some code may be not regenerated
  */
@@ -238,3 +240,11 @@ export function applyLLMPatch(source: string, chunks: Chunk[]): string {
 
     return lines.filter((l) => l !== undefined).join("\n")
 }
+
+export function renderUnifiedDiff(diff: string) {
+    const res: string[] = []
+    const files = parseUniDiff(diff);
+    for(const file of files) {
+        
+    }
+}
\ No newline at end of file
