{
  "name": "gptools-sample",
  "version": "1.3.0",
  "license": "MIT",
  "private": true,
  "devDependencies": {
    "typescript": "^5.3.3"
  },
  "scripts": {
    "go": "node ../cli/built/main.js src/sample.ts",
    "token": "mkdir -p .gptools/tmp; (if test -e /dev/clipboard; then cat /dev/clipboard ; else pbpaste ; fi) > .gptools/tmp/request.txt",
    "typecheck": "tsc -p src"
  },
  "dependencies": {
    "sharp": "^0.32.2"
  }
}
