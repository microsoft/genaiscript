---
import AudioPlayer from "./AudioPlayer.astro"

interface Props {}

const { url } = Astro
const audioUrl = url.href.replace(/\/$/, "") + ".mp3"
---

<AudioPlayer aiDisclaimer={true} src={audioUrl} />
