---
title: Llamafile
sidebar:
  order: 50
llmstxt:
  content: Llamafile is a standalone desktop application for running LLMs locally.
    The provider is `llamafile`, and the model name parameter is not required.
    Visit https://llamafile.ai/ for more details.
  hash: a25db5e32b81ac0276c004dec44d11691fba0d42325d321fd40b0f4eac1f609a

---

import { FileTree } from "@astrojs/starlight/components";
import { Steps } from "@astrojs/starlight/components";
import { Tabs, TabItem } from "@astrojs/starlight/components";
import { Image } from "astro:assets";
import LLMProviderFeatures from "../../../components/LLMProviderFeatures.astro";

[https://llamafile.ai/](https://llamafile.ai/) is a single file desktop application
that allows you to run an LLM locally.

The provider is `llamafile` and the model name is ignored.
