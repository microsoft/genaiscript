---
title: AST Markdown
description: Utilisez les assistants mdast pour analyser et manipuler des documents Markdown
sidebar:
  order: 80

---

import { PackageManagers } from "starlight-package-managers";

Ces aides d’exécution fournissent une interface conviviale autour de l’écosystème [remark](https://github.com/remarkjs/remark), [mdast](https://github.com/syntax-tree/mdast), [unified](https://github.com/syntax-tree/unist) pour analyser et manipuler des documents Markdown.

Pour bénéficier de l’auto-complétion des types, vous devrez installer le paquet `@types/mdast` comme dépendance de développement.

<PackageManagers pkg="@types/mdast" dev />

## Manipulation du Markdown

* charger les analyseurs

```typescript
import { mdast } from "@genaiscript/runtime";

const { parse, visit, stringify } = await mdast();
```

* analyser vers un arbre mdast

```typescript
const root = parse("# Hello World");
```

* parcourir l’arbre (voir [documentation](https://unifiedjs.com/learn/recipe/tree-traversal/pnp))

```typescript
const updated = visit(root, `code`, (node) => {
  ...node
});
```

* sérialiser l’arbre en Markdown

```typescript
const markdown = await stringify(updated);
```

<hr />

Traduit par IA. Veuillez vérifier l’exactitude du contenu.
